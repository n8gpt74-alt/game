# Улучшения системы сохранения игрового прогресса

## ✅ Что было сделано

### 1. Мягкая деградация состояния питомца

**Проблема**: При длительном отсутствии питомец деградировал слишком сильно.

**Решение**: Реализована адаптивная система деградации:
- **0-6 часов**: Нормальная скорость деградации (100%)
- **6-24 часа**: Замедленная деградация (50% скорости)
- **>24 часов**: Минимальная деградация (25% скорости)

**Изменения**:
- Снижена базовая скорость деградации (0.8x вместо 1.0x)
- Уменьшены штрафы за низкие показатели
- Здоровье падает только при критических значениях (<25 вместо <30)
- Меньший штраф за одиночество (1.5x вместо 1.7x)

**Файл**: `backend/app/services/simulation.py`

### 2. Улучшенная загрузка данных

**Проблема**: При входе данные загружались только с сервера, кэш не использовался.

**Решение**: Реализована стратегия Cache-First:
1. При старте приложения данные загружаются из localStorage **мгновенно**
2. В фоне происходит синхронизация с сервером
3. После синхронизации UI обновляется серверными данными
4. Пользователь видит уведомление о синхронизации

**Файл**: `frontend/src/App.tsx`

### 3. Индикаторы синхронизации

**Добавлено**:
- Индикатор "синхронизация..." в шапке приложения
- Индикатор "офлайн" при отсутствии сети
- Уведомления о статусе синхронизации:
  - "Прогресс загружен (X мин/часов назад)"
  - "✓ Прогресс синхронизирован"
  - "⚠ Работаем из сохранённых данных"

**Файлы**: 
- `frontend/src/App.tsx`
- `frontend/src/styles.css`

### 4. Автоматическое сохранение

**Что сохраняется**:
- ✅ Состояние питомца (уровень, опыт, ресурсы, статы)
- ✅ История действий (последние 30 событий)
- ✅ Ежедневные задания и прогресс
- ✅ Инвентарь и экипировка
- ✅ Каталог магазина
- ✅ Timestamp последнего сохранения

**Когда сохраняется**:
- После каждого действия игрока
- При изменении любого состояния
- Автоматически в localStorage

**Файл**: `frontend/src/offlineCache.ts`

## 📊 Результаты тестирования

Все тесты пройдены успешно:

```
✓ 2 часа отсутствия: голод=70, энергия=75, чистота=70, настроение=77
✓ 12 часов отсутствия: голод=66, энергия=71, чистота=66, настроение=76
✓ 48 часов отсутствия: голод=73, энергия=78, чистота=73, здоровье=85
✓ 30 часов с одиночеством: настроение=77, голод=73
✓ Здоровье стабильно при некритических значениях
```

**Файл**: `backend/tests/test_gentle_decay.py`

## 🎮 Как это работает для игрока

### Сценарий 1: Короткий перерыв (1-2 часа)
1. Игрок выходит из игры
2. Прогресс автоматически сохраняется
3. Игрок возвращается через 2 часа
4. Данные загружаются мгновенно из кэша
5. В фоне синхронизируются с сервером
6. Питомец немного проголодался, но в хорошем состоянии

### Сценарий 2: Средний перерыв (6-12 часов)
1. Игрок выходит на ночь
2. Возвращается утром
3. Видит сообщение "Прогресс загружен (8ч назад)"
4. Питомец умеренно проголодался (деградация замедлена)
5. Все ресурсы и уровень сохранены

### Сценарий 3: Длительный перерыв (>24 часов)
1. Игрок не заходил несколько дней
2. Возвращается
3. Питомец "скучает" (is_lonely = true)
4. Деградация минимальная (25% скорости)
5. Питомец в приемлемом состоянии
6. Все достижения сохранены

### Сценарий 4: Офлайн-режим
1. Потеря интернета
2. Показывается "⚠ офлайн"
3. Игра работает с локальными данными
4. Все действия сохраняются локально
5. При восстановлении сети - автоматическая синхронизация

## 🔧 Технические детали

### Формула деградации

```python
# Коэффициент замедления
if hours_elapsed <= 6:
    decay_multiplier = 1.0    # 100% скорости
elif hours_elapsed <= 24:
    decay_multiplier = 0.5    # 50% скорости
else:
    decay_multiplier = 0.25   # 25% скорости

# Применение деградации
ticks = (effective_seconds / 600.0) * decay_multiplier
hunger -= 0.8 * ticks
energy -= 0.8 * ticks
hygiene -= 0.8 * ticks
```

### Структура кэша

```typescript
type ЛокальныйСнимок = {
  state: СостояниеПитомца | null;
  history: ЗаписьСобытия[];
  daily: СостояниеЗаданий | null;
  catalog: КаталогМагазина;
  inventory: ПредметИнвентаря[];
  savedAt: string;  // ISO timestamp
};
```

### Ключ localStorage

```typescript
const SNAPSHOT_KEY = "дракончик_искра_кэш_v1";
```

## 🚀 Как запустить

### Запуск тестов

```powershell
# Тесты деградации
cd backend
python tests/test_gentle_decay.py

# Все тесты
python -m pytest tests -q
```

### Локальный запуск

```powershell
# Бэкенд
cd backend
.\.venv\Scripts\Activate.ps1
$env:DATABASE_URL='sqlite:///./local_run.db'
$env:ALLOW_DEV_AUTH='true'
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000

# Фронтенд (в другом терминале)
cd frontend
$env:VITE_API_BASE='http://127.0.0.1:8000'
npm run dev -- --host 127.0.0.1 --port 5173
```

### Docker Compose

```powershell
docker compose up -d --build
```

## 📝 Что НЕ деградирует

Эти данные **НИКОГДА** не теряются:
- ✅ Уровень питомца
- ✅ Опыт (XP)
- ✅ Монеты
- ✅ Интеллект
- ✅ Кристаллы
- ✅ Инвентарь
- ✅ Экипировка
- ✅ История действий
- ✅ Прогресс заданий

Деградируют только **временные** показатели:
- ⚠️ Голод (hunger)
- ⚠️ Энергия (energy)
- ⚠️ Чистота (hygiene)
- ⚠️ Настроение (happiness)
- ⚠️ Здоровье (health) - только при критических значениях

## 🎯 Преимущества новой системы

1. **Мгновенная загрузка**: Данные показываются сразу из кэша
2. **Мягкая деградация**: Питомец не умирает за ночь
3. **Офлайн-режим**: Игра работает без интернета
4. **Прозрачность**: Игрок видит статус синхронизации
5. **Надёжность**: Данные сохраняются автоматически
6. **Справедливость**: Длительное отсутствие не наказывается слишком сильно

## 🔮 Будущие улучшения

Возможные дополнения (не реализованы):
- Сжатие данных в localStorage
- Более умная очередь синхронизации
- Мультиустройство (синхронизация между устройствами)
- Экспорт/импорт прогресса
- Резервное копирование на сервере

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте localStorage (Application → Local Storage)
3. Проверьте логи сервера
4. Очистите кэш и перезагрузите страницу

## ✨ Итог

Теперь **ВСЕ данные сохраняются** после выхода из игры:
- Прогресс загружается мгновенно
- Деградация стала мягче и справедливее
- Игра работает офлайн
- Игрок видит статус синхронизации
- Ничего не теряется!
