# Тестирование визуальных эффектов

## Статус
✅ **Система полностью интегрирована и готова к тестированию**

## Как открыть игру
Открой в браузере: **http://localhost:5173/**

## Что тестировать

### 1. Цикл день/ночь (24 минуты реального времени)
- **Утро (0-6 мин)**: Небо розово-оранжевое, солнце восходит
- **День (6-12 мин)**: Яркое голубое небо, солнце в зените
- **Вечер (12-18 мин)**: Небо оранжево-фиолетовое, солнце садится
- **Ночь (18-24 мин)**: Темное небо, луна, мерцающие звезды

**Как ускорить тест**: Открой консоль браузера (F12) и введи:
```javascript
// Ускорить время в 60 раз (1 минута = 1 секунда)
window.visualEffects?.timeManager?.setTimeScale(60);

// Установить конкретное время (0-1, где 0.5 = полдень)
window.visualEffects?.timeManager?.setTimeOfDay(0.75); // Вечер
```

### 2. Погода (случайная)
- Каждые 2 минуты есть 15% шанс дождя или снега
- Длительность: 1-3 минуты
- **Дождь**: Синие капли падают сверху вниз
- **Снег**: Белые снежинки медленно падают

**Как принудительно запустить**:
```javascript
// Дождь
window.visualEffects?.weatherSystem?.startWeather('rain');

// Снег
window.visualEffects?.weatherSystem?.startWeather('snow');

// Остановить
window.visualEffects?.weatherSystem?.stopWeather();
```

### 3. Анимации окружения
- **Деревья**: Плавно качаются (период 2-3 сек)
- **Трава**: Волны (период 1.5-2 сек)
- **Бабочки**: 3-5 штук летают вокруг (случайные траектории)
- **Падающие листья**: 10-15 листьев медленно падают и исчезают

### 4. Эффекты действий
Нажми на кнопки действий внизу экрана:

- **Покормить** 🍎: Искры золотого цвета
- **Помыть** 🛁: Синие пузыри воды
- **Поиграть** 🎮: Разноцветные конфетти
- **Вылечить** 💊: Зеленые сердечки
- **Поговорить** 💬: Розовые сердечки
- **Спать** 😴: Фиолетовые звездочки
- **Убрать** 🧹: Белые искры

Каждое действие также создает:
- Вспышку света (1 секунда)
- Частицы вокруг дракона
- Анимацию самого дракона

### 5. Производительность
Система автоматически адаптируется:
- Если FPS < 45 в течение 3 секунд → снижает качество
- На мобильных (ширина < 768px) → средний уровень качества
- Настройки сохраняются в localStorage

**Проверить FPS**:
```javascript
// Показать текущий FPS
console.log(window.visualEffects?.performanceManager?.getCurrentFPS());

// Показать текущий уровень качества
console.log(window.visualEffects?.performanceManager?.getQualityLevel());
```

## Консоль разработчика
Открой консоль (F12) и увидишь:
```
[VisualEffects] Initialized
[VisualEffects] Started
[TimeManager] Time of day: 0.25 (утро)
[WeatherSystem] Started rain
[ParticleEmitter] Triggered feed effect
```

## Известные особенности
1. Первый запуск может занять 2-3 секунды (загрузка 3D модели)
2. Эффекты частиц исчезают через 2-3 секунды
3. Погода меняется случайно, может не появиться сразу
4. Цикл день/ночь очень медленный (24 минуты), используй консоль для ускорения

## Если что-то не работает
1. Открой консоль (F12) и проверь ошибки
2. Обнови страницу (Ctrl+R)
3. Очисти кэш (Ctrl+Shift+R)
4. Проверь что backend запущен на http://localhost:8000

## Настройки качества
По умолчанию система выбирает качество автоматически. Можно изменить вручную:

```javascript
// Низкое качество (100 частиц, без теней)
window.visualEffects?.performanceManager?.setQualityLevel('low');

// Среднее качество (150 частиц, тени включены)
window.visualEffects?.performanceManager?.setQualityLevel('medium');

// Высокое качество (200 частиц, тени, антиалиасинг)
window.visualEffects?.performanceManager?.setQualityLevel('high');
```

---

**Приятного тестирования! 🎮✨**
