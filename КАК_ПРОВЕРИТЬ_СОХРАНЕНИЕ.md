# Как проверить, что всё сохраняется

## Быстрая проверка

### 1. Запустить приложение

```powershell
# Бэкенд
cd backend
.\.venv\Scripts\Activate.ps1
$env:DATABASE_URL='sqlite:///./local_run.db'
$env:ALLOW_DEV_AUTH='true'
$env:SECRET_KEY='dev-secret'
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000

# Фронтенд (в другом терминале)
cd frontend
$env:VITE_API_BASE='http://127.0.0.1:8000'
npm run dev -- --host 127.0.0.1 --port 5173
```

### 2. Открыть в браузере

Открой: `http://127.0.0.1:5173`

### 3. Выполнить действия

1. Покорми питомца несколько раз
2. Поиграй с ним
3. Посмотри на уровень и ресурсы
4. Запомни значения

### 4. Проверить localStorage

Открой консоль браузера (F12) → Application → Local Storage → `http://127.0.0.1:5173`

Найди ключ: `дракончик_искра_кэш_v1`

Должен быть JSON с:
```json
{
  "state": {
    "level": 1,
    "xp": 25,
    "coins": 50,
    "hunger": 85,
    ...
  },
  "history": [...],
  "daily": {...},
  "savedAt": "2026-02-13T..."
}
```

### 5. Закрыть и открыть заново

1. Закрой вкладку браузера
2. Открой снова `http://127.0.0.1:5173`
3. Должно появиться сообщение: "Прогресс загружен (X мин назад)"
4. Все данные должны быть на месте!

### 6. Проверить синхронизацию

В шапке приложения должно быть:
- "Дракончик Искра"
- "Малыш • синхронизация..." (на несколько секунд)
- Потом просто "Малыш"

Через несколько секунд появится: "✓ Прогресс синхронизирован"

## Проверка деградации

### 1. Изменить время последнего тика

Открой консоль браузера (F12) → Application → Local Storage

Найди `дракончик_искра_кэш_v1` и отредактируй:

```json
{
  "state": {
    ...
    "last_tick_at": "2026-02-12T10:00:00Z"  // Вчерашняя дата
  }
}
```

### 2. Перезагрузить страницу

Питомец должен немного проголодаться, но не критично!

### 3. Проверить значения

После 24 часов отсутствия:
- Голод должен быть > 40
- Энергия должна быть > 40
- Здоровье должно быть > 70
- Уровень и ресурсы БЕЗ ИЗМЕНЕНИЙ

## Проверка офлайн-режима

### 1. Отключить интернет

В DevTools (F12) → Network → Throttling → Offline

### 2. Выполнить действия

Покорми питомца, поиграй с ним

### 3. Проверить индикатор

В шапке должно быть: "Малыш • офлайн"

Должно появиться: "⚠ Работаем из сохранённых данных"

### 4. Включить интернет

Network → Throttling → No throttling

Должно появиться: "✓ Прогресс синхронизирован"

## Проверка тестов

```powershell
cd backend
python tests/test_gentle_decay.py
```

Должно вывести:
```
✓ 2 часа: голод=70, энергия=75, чистота=70, настроение=77
✓ 12 часов: голод=66, энергия=71, чистота=66, настроение=76
✓ 48 часов: голод=73, энергия=78, чистота=73, здоровье=85
✓ 30 часов (одиночество): настроение=77, голод=73
✓ Здоровье стабильно: 85

✅ Все тесты пройдены!
```

## Что проверять

### ✅ Должно работать:
- Мгновенная загрузка из кэша
- Сохранение после каждого действия
- Синхронизация с сервером в фоне
- Индикаторы "синхронизация..." и "офлайн"
- Уведомления о статусе
- Мягкая деградация (не слишком сильная)
- Сохранение уровня, опыта, ресурсов
- Офлайн-режим

### ❌ НЕ должно происходить:
- Потеря уровня или опыта
- Потеря монет, интеллекта, кристаллов
- Потеря инвентаря
- Критическая деградация за ночь
- Ошибки при офлайне
- Потеря данных при перезагрузке

## Проблемы и решения

### Проблема: Данные не загружаются

**Решение**: Проверь localStorage в DevTools. Если пусто - выполни несколько действий, они сохранятся автоматически.

### Проблема: Питомец слишком сильно деградировал

**Решение**: Проверь, что изменения в `simulation.py` применены. Перезапусти бэкенд.

### Проблема: Не показывается индикатор синхронизации

**Решение**: Проверь, что изменения в `App.tsx` применены. Перезапусти фронтенд.

### Проблема: Ошибка при офлайне

**Решение**: Это нормально! Приложение должно работать с локальными данными. Проверь, что действия выполняются.

## Итог

Если всё работает правильно:
1. ✅ Данные загружаются мгновенно
2. ✅ Всё сохраняется автоматически
3. ✅ Деградация мягкая и справедливая
4. ✅ Офлайн-режим работает
5. ✅ Индикаторы показывают статус
6. ✅ Ничего не теряется!

**Готово! Теперь весь игровой процесс сохраняется после выхода из игры! 🎉**
