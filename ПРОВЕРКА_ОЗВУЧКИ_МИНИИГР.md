# Smoke-check: озвучка буквенных мини-игр в Telegram WebView

## Цель
Проверить, что буквенные мини-игры работают стабильно в Telegram Mini App:
- озвучка через Web Speech API срабатывает, когда доступна;
- при недоступности/сбое API показывается текстовый fallback;
- игровой поток не блокируется (таймер/кнопки/переходы между раундами работают).

---

## Подготовка
- Устройство с Telegram (Android и/или iOS).
- Включён звук медиа на устройстве.
- В Telegram не включён беззвучный режим для приложения.
- Открыта актуальная версия мини-приложения.

---

## Базовый сценарий (позитив)
1. Открыть `Мини-игры` → выбрать категорию `Буквы`.
2. Запустить игру `Буква на слух`.
3. Убедиться, что при старте раунда звучит буква или слово.
4. Нажать `🔊 Повторить озвучку` 2-3 раза подряд.
5. Проверить, что:
   - озвучка повторяется;
   - кнопки ответов остаются активны;
   - можно дать ответ без задержек.
6. Пройти 5 раундов и забрать награду.

Ожидание:
- Озвучка слышна.
- Нет зависания UI.
- Результат отправляется, награда начисляется.

---

## Негативный сценарий (fallback)
1. Запустить любую буквенную игру (`Буква на слух`, `Первая буква`, `Пропущенная буква`).
2. Если на устройстве озвучка не воспроизводится (или Web Speech API не поддерживается), проверить наличие текстовой подсказки вида:
   - `Слушай: А`
   - `Слушай слово: КОТ`
3. Нажать `Повторить озвучку`.

Ожидание:
- Игра не ломается.
- Появляется/остаётся текстовый fallback.
- Раунд можно пройти до конца.

---

## Проверка неблокирующего поведения
1. В буквенной игре быстро нажимать `Повторить озвучку` и сразу выбирать ответ.
2. Повторить на нескольких раундах.

Ожидание:
- Нажатия принимаются.
- Переключение раундов происходит штатно.
- Нет «залипания» кнопок и потери кликов.

---

## Проверка прогресса по категориям
1. Сыграть 1 игру в категории `Математика`.
2. Сыграть 1 игру в категории `Буквы`.
3. Открыть экран `Задания`, затем `Достижения`.

Ожидание:
- Увеличивается общий `minigame_count`.
- Отдельно растут:
  - `math_minigame_count`
  - `letters_game_count`
- В достижениях прогресс категорий не смешивается.

---

## Кросс-платформенный минимум
Пройти базовый сценарий минимум на:
- Android Telegram WebView;
- iOS Telegram WebView (если доступно);
- Desktop (Telegram Desktop/Web) — как дополнительная проверка.

---

## Что логировать при проблеме
Если найден дефект, фиксировать:
- устройство/ОС/версия Telegram;
- какая именно игра и раунд;
- было ли слышно аудио;
- был ли fallback-текст;
- воспроизводимость (всегда/иногда);
- скриншот/видео + время события.

---

## Критерии прохождения smoke
Smoke считается пройденным, если:
- буквенные игры запускаются и завершаются без ошибок;
- озвучка работает либо корректно срабатывает fallback;
- UI остаётся отзывчивым;
- прогресс `math/letters` обновляется раздельно.
